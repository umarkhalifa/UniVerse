class UserEntity {
  final String? userName;
  final int ? color;
  final String? avatar;
  final String? department;
  final String? level;

  UserEntity(
      this.userName, this.color, this.avatar, this.department, this.level);
}
